// Function to greet someone by name
function greet(name) {
    return "Hello, ".concat(name, "!"); // Uses template literals for dynamic greeting
}
var user = "M HammadurRehman"; // Declares variable user with type string
console.log(greet(user)); // Calls greet function with user and logs the result
